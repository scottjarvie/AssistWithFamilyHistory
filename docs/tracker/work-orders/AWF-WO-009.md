---
id: AWF-WO-009
title: Prove the joined newcomer Queue-to-sourced-result workflow
execution: ready
audit: not-audited
cards: AWF-0037
created: 2026-08-13
updated: 2026-08-14
proposed-by: Codex from Scott's direct delegation
approved-by: Scott via coordinator delegation
approved-on: 2026-08-13
executed-by: Codex
---

## Goal

Prove and improve one complete soft-launch newcomer loop: begin in an empty
private workspace, leave a minimal research directive, let an explicitly
connected chosen AI read the owner-scoped context and preserve one sourced
result, then return through Queue into the canonical signed-in person/story UI.

## Authority and scope

Scott directly authorized the joined live acceptance, clearly marked synthetic
fixtures, routine temporary account/session/client work, small reproduced
first-start/Queue/MCP/visibility/rebrand repairs, protected release, deployment,
exact cleanup, tracker/release truth, and worktree closure. Use the existing
retained empty non-privileged test identity rather than creating or using a real
family account.

## Sequence

1. Read the canonical Project Philosophy, tracker, current auth/tenant model,
   schema, Queue/MCP/UI routes, deployment configuration, and prior receipts.
2. Reproduce the narrowest joined newcomer defect and build exact owner-scoped,
   marker-scoped cleanup before any live family mutation.
3. Implement the smallest product-correct bridge and focused adversarial tests.
4. Run full local verification and responsive synthetic browser proof.
5. Release through a normal protected PR; verify PR CI, exact-main CI, Convex,
   Vercel, and the canonical production alias.
6. Use one disposable public PKCE OAuth client with explicit minimal consent to
   run the official MCP client against the retained test identity.
7. Confirm Queue and canonical UI visibility, then remove the exact fixtures,
   client, sessions, connection artifacts, and local secrets.
8. Publish exact Current/Partial/Later tracker and release evidence and leave the
   worktree clean.

## Dependencies

- Assist With Family History Project Philosophy 1.7.0 and shared Core v1.6.3.
- Version 2.0.1 canonical first journey, AWF-WO-006 stateless MCP foundation,
  and AWF-WO-005 Queue foundation.
- Retained labeled non-privileged Clerk identity, current minimal Convex token
  template, normal canonical domain, and disposable named PKCE client path.

## Exclusions

- No Scott or real-family records, living-person material, cross-family reads,
  identity merge, FamilySearch merge, sharing, story publication, or role
  elevation.
- No provider defaults/policy change, broad scopes, secret exposure, billing,
  DNS, new durable client, or weakening of issuer/signature/subject checks.
- No claim that one disposable client proves all named clients, refresh,
  reconnect, fresh-device email verification, or immediate JWT revocation.

## Stop rules

Stop for cross-family access, real family data, a provider-policy or security
change beyond the existing disposable PKCE path, secret exposure, cleanup that
cannot resolve to the exact marked graph, or any irreversible public action.

## Human gates

Scott's direct delegation approves the bounded implementation, disposable live
identity/client/session flow, protected release, deployment, fixture cleanup,
and tracker/release publication. No further routine approval is needed inside
the exclusions.

## Verification

- focused Queue result-link, MCP transport, tenant, and acceptance-cleanup tests
- `pnpm verify`
- local Queue result presentation at phone and desktop widths
- protected PR, PR-head CI/preview, exact-main CI, Convex deployment, and Vercel
  production proof
- canonical signed-out/public/OAuth discovery and fail-closed anonymous MCP
- official MCP client: brief → Queue detail/claim → source/result save → Queue
  completion → normal UI visibility
- exact zero-fixture, zero-disposable-client, zero-session, and artifact cleanup

## Current truth

Execution is ready to resume and audit remains `not-audited`. The source repair,
cleanup rail, full verification, protected review, exact-main CI, Convex deploy,
Vercel production deploy, and public boundary proof are complete. The final
authenticated joined lifecycle is blocked only by the available isolated
browser hosts: local Chromium cannot start inside the macOS sandbox, and the
sanctioned in-app runner hung past one-use-token expiry before returning a
signed-out empty route. No fixture was created; both attempts ended with zero
active exact-user sessions and deleted disposable clients.

## Execution evidence

The implementation preserves the existing product model rather than adding raw
CRUD: server-derived identity, bounded reads, replay-safe writes, canonical
source/citation/finding/story records, and the exact four-state Queue. The
result-link bridge accepts only known signed-in app routes. The user-authenticated cleanup
rail is inert outside production deployment `accomplished-dodo-308`, the exact
retained test subject, and a unique marked run key; it also refuses external
references and bounded-scan overflow.

`pnpm verify` passed 47/47. PR #48 merged as `3da3d4a`, PR #49 as `915f99f`,
and the Convex generated-type repair PR #50 as `58a7918`. Exact-main Actions
`31772638150` passed, and production deployment
`dpl_FRzBKunKnoJUtVRNfbJmqzcUr2JN` reached Ready for `58a7918`. The canonical
public site, AI guide, updates, OAuth discovery, and anonymous MCP resource
challenge passed. Detailed proof and the remaining gate are recorded in
`docs/operations/family-history-joined-newcomer-acceptance-2026-08-13.md`.

## History

- 2026-08-14 · Codex portfolio reset — moved execution from Active to Ready
  because no worker can currently complete the browser-host acceptance; kept
  the finished release evidence and exact cleanup boundary intact.
- 2026-08-13 · Scott via coordinator delegation — authorized the bounded goal
  and routine safe live proof through cleanup and protected release.
- 2026-08-13 · Codex — activated execution after full repo/product orientation;
  protected delivery and live proof continue.
- 2026-08-13 · Codex — completed protected production delivery and public proof;
  exact authenticated acceptance remains environment-blocked after safe cleanup.

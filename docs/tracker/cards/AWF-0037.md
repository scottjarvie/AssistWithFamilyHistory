---
id: AWF-0037
title: Join a newcomer Queue handoff to a sourced chosen-AI result
status: next
type: product
area: First Journey & Chosen AI
priority: P0
horizon: Now
gap-kind: joined Queue, MCP, and normal-UI acceptance gap
verdict: bridge is live; authenticated joined acceptance is environment-blocked
contract: "private newcomer directive, owner-scoped chosen AI, sourced durable result, and visible Queue handback"
work-orders: AWF-WO-009
created: 2026-08-13
updated: 2026-08-14
updated-by: Codex
---

## Why this exists

The product has separately proven an empty signed-in first start, a durable
four-state Queue, and an owner-scoped OAuth MCP workspace. It has not yet proven
the useful joined loop: a newcomer leaves a private research directive, their
chosen AI deliberately claims it, reads only that owner's context, preserves a
sourced result in canonical records, and hands the result back through Queue
into the normal signed-in UI.

## Current truth

Source inspection reproduced one concrete seam before any production fixture
was created. `update_queue` already accepts durable `resultRefs`, but Queue
renders only the result summary and never presents those references. A user can
therefore see **Done** without an actionable route to the person or private
story the AI saved.

The bounded implementation now turns only known signed-in Family History paths
into **Open saved work** links and teaches `update_queue` callers to return those
paths. External, malformed, settings, and opaque legacy references remain
non-clickable. A production-only, user-authenticated acceptance cleanup rail is restricted
to the exact retained non-privileged test subject, exact Convex deployment,
unique `codex-test:awf-joined:` run keys, bounded scans, and an explicit
confirmation phrase and the exact labeled test identity. It refuses cleanup if any unmarked record reuses the
synthetic graph.

The bridge and exact cleanup rail are live through protected PRs #48, #49, and
#50. Exact-main CI and production deployment passed. Authenticated joined
acceptance remains unproved because both available isolated browser runners
failed before a usable product session: local Chromium was denied by the macOS
Mach-port sandbox, while the sanctioned in-app runner hung until its one-use
token expired and then reached the signed-out route with an empty body. Both
attempts deleted the disposable client and returned the retained identity to
zero active sessions; no production family or Queue fixture was created.

## Next safe action

Resume only when a sanctioned browser host can complete the retained synthetic
identity sign-in. Run the already-bounded Queue → MCP → sourced save → Queue/UI
visibility flow, then remove the exact marked graph, Queue/activity/receipt
rows, client, session, and local artifacts. Do not broaden this into new server,
provider, or product work unless acceptance reproduces a real defect.

## Current / Partial / Later

- **Current:** the saved-work bridge, exact cleanup rail, stateless MCP tools,
  Queue, and normal UI are live.
- **Partial:** the joined authenticated browser lifecycle remains unproved only
  because both available isolated browser hosts failed before a usable session.
- **Later:** additional clients, reconnect, refresh, mobile setup, and immediate
  token revocation remain separate compatibility/security work.

## Boundaries

- Use only the retained labeled non-privileged test identity and one unique,
  clearly marked synthetic deceased person/evidence/story graph.
- Derive owner identity from the authenticated MCP token; never accept an owner
  supplied by the chosen AI.
- Do not inspect Scott's or another family's records, publish/share a story,
  merge identities, change roles, or change provider defaults.
- Delete the exact Queue, family, MCP receipt/activity, OAuth client, session,
  and local credential artifacts after each live run; retain only the empty
  labeled test identity.
- Treat independent audit and immediate issued-JWT revocation as separate work.

## Definition of done

- [ ] A signed-in newcomer can leave one marked private research directive.
- [ ] A disposable OAuth PKCE chosen-AI client can read the exact owner's brief
      and Queue, claim the directive, and save one citation-backed result.
- [ ] Queue shows the attributable completion and opens the saved person/story
      in the normal signed-in UI at phone and desktop widths.
- [x] Cross-owner access, arbitrary result-link navigation, and broad cleanup
      fail closed in focused tests.
- [ ] Full verification, a protected PR, exact-main CI, production deployment,
      live public/OAuth boundary checks, and authenticated lifecycle proof pass.
- [ ] Exact fixture/client/session/artifact cleanup returns the labeled test
      identity to an empty private workspace.

## Evidence

- 2026-08-13 source orientation: version 2.0.1, AWF-WO-006 MCP foundation,
  AWF-WO-008 first-start release, Queue UI/API, MCP tool schemas, tenant guards,
  canonical vault routes, and prior production acceptance record inspected from
  clean `origin/main` commit `e28e379a`.
- 2026-08-13 reproduced source defect: `QueueItemRecord.resultRefs` is persisted
  and returned, but `QueueDetail` has no result-reference rendering.
- `pnpm verify` passed all 47 checks. PR #48 merged as `3da3d4a`, PR #49 as
  `915f99f`, and the Convex code-generation correction PR #50 as `58a7918`.
  PR and exact-main CI passed; production deployment
  `dpl_FRzBKunKnoJUtVRNfbJmqzcUr2JN` reached Ready for `58a7918` and serves the
  canonical alias.
- Public home, `/ai`, `/updates`, OAuth protected-resource discovery, and the
  fail-closed anonymous MCP challenge passed after deployment.
- Two authorized isolated acceptance attempts failed before sign-in completed.
  Each removed its disposable PKCE client/token artifacts and ended with zero
  active exact-user sessions and zero marked fixture rows. The browser-host
  limitation, not product source or GitHub publication, is the remaining gate.

## History

- 2026-08-13 · Scott via coordinator delegation — authorized this bounded
  joined workflow, disposable synthetic account/session work, in-scope repair,
  protected release, deployment, live proof, cleanup, and durable truth.
- 2026-08-13 · Codex — activated the Card after philosophy, tracker, auth,
  schema, Queue/MCP, deployment, and prior-release orientation.
- 2026-08-13 · Codex — shipped the bridge and cleanup boundary through three
  protected PRs and production, then stopped honestly at the isolated browser
  host gate after exact zero-fixture/client/session cleanup.
- 2026-08-14 · Codex portfolio reset — moved the Card from Doing to Next because
  no executor is currently able to run the browser acceptance; retained the
  exact host limitation and cleanup boundary without inventing a human blocker.

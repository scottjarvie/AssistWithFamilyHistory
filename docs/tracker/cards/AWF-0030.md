---
id: AWF-0030
title: Prove deployed Queue persistence and chosen-AI boundary
status: backlog
type: verification-gap
area: Research Operations & Agent Handoff
priority: P1
horizon: Now
gap-kind: verification gap
verdict: unverified
contract: "Core §§4, 5, 7, 16.3 · authenticated Queue deployment proof"
dependencies: AWF-0008, AWF-0012
created: 2026-08-09
updated: 2026-08-12
updated-by: Codex
---

## Why this exists

Source, tests, full CI, Vercel builds, the live alias, and signed-out route
protection are proven. They do not prove that the production Convex build took
the deploy-key branch or that a real authenticated Queue round trip persists
with the expected tenant boundary.

## Verified current truth

PR #31 merged the original Queue source as `e65b03a`. The P0 tenancy and expiry
repair later merged through PR #32 as
`1a82a03ed207c8cd3c9e99f0dd4b6d4720c282cd`; exact-main Actions run
`31615964828` passed and Vercel production deployment
`HDzMFmbrsany1z95y6y63r3vSHgb` reported Ready and Current for that commit. A
fresh signed-out public probe returned HTTP 200 for `/`, but `/api/queue`
resolved to a Clerk `protect-rewrite` 404 with signed-out headers rather than
the previously recorded sign-in redirect. No authenticated Queue request,
cross-tenant production fixture, or exact Convex provider build-plan line was
available.

## Scope

- Inspect the exact production Vercel build log and record whether
  `scripts/vercel-build.mjs` selected the production Convex deploy branch.
- With a safe synthetic account, create a directive-only Queue item, read it
  back, exercise one allowed transition, inspect attributable activity, delete
  it, and prove a second tenant cannot read it.
- If a chosen-AI credential path exists by then, prove server-derived identity
  and one narrow handoff. Otherwise record the connection as unavailable rather
  than substituting an internal call.
- Do not inspect, modify, or report real user genealogy data.

## Next safe action

Use isolated synthetic authenticated test access and inspect the exact provider
build log without changing account-security settings. First reconcile the
signed-out 404 against the intended honest API behavior. Stop at sign-in, MFA,
provider account changes, or real production-data scope rather than bypassing
them.

## Evidence

Acceptance evidence:

- Exact Vercel deployment id, main commit, build-plan line, and Convex target.
- Dated synthetic item/activity ids with directive-only create, transition,
  delete, and cross-tenant denial results; redact credentials and private data.
- Honest chosen-AI result: connected and narrowly proven, or unavailable with
  the precise missing boundary.

## Unknown/access limitation

The 2026-08-12 remediation task had an authenticated Vercel deployment overview
and signed-out public access. A provider account-security setup prompt blocked
the build-log panel; changing or dismissing that account setting was outside
scope. No safe authenticated test identity or external chosen-AI credential was
used. Those are access gates, not evidence that provider/authenticated behavior
passed or failed. The observed signed-out 404 is current evidence and requires
separate reconciliation.

## History

- 2026-08-09 · Codex — separated the remaining production/authenticated proof
  from completed Queue foundation execution after PR #31 and live signed-out
  verification.
- 2026-08-12 · Codex — recorded PR #32 exact-main CI and Vercel production
  readiness, corrected signed-out `/api/queue` truth to the observed Clerk 404,
  and preserved provider/authenticated proof as an access-bounded follow-up.

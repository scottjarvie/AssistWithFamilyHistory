---
id: AWF-0042
title: Let a person understand, narrow, and revoke each chosen-AI connection
status: done
type: product-change
area: Product Experience
priority: P0
horizon: Next
gap-kind: human connection and consent experience
verdict: public setup guidance exists; owner-visible connection, scope, activity, expiry, and revoke controls do not
contract: "/ai education + authenticated connection center + generated AI guide + manual Queue fallback"
dependencies: AWF-0040, AWF-0041
created: 2026-08-16
updated: 2026-08-16
updated-by: Claude (execution)
work-orders: AWF-WO-011
---

## Why this exists

A person should not have to understand OAuth plumbing to know which AI can see
which family material, what it may change, when it last worked, and how to stop
it. The connection experience is part of the privacy product, not merely setup
documentation.

## Current truth

`/ai` and `/ai.txt` provide the branded endpoint, compatible-client capability
requirement, first call, twelve-tool catalog, one-call save guidance, safety,
stale-tool recovery, and Current/Partial/Later truth. They do not expose a
client-specific setup recipe without proof, which is correct.

There is no authenticated `/settings/ai` connection center, scope picker,
record-boundary selector, connected-client list, last use, safe activity,
expiry, or immediate revoke control. The older API-key management UI is a
separate credential system and does not prove OAuth connection management.

## Requirements

- Keep `/ai` calm, outcome-first, client-neutral, and truthful.
- Add an authenticated connection center using the app's signed-in navigation
  and Family History design character.
- Show client identity/provenance, selected record boundary, read/write scopes,
  consent time, expiry, last use, recent safe activity, and status.
- Let the person reduce or revoke a grant with immediate, clearly explained
  effect.
- Explain that Queue context is continuity, not blanket record authority.
- Keep a copyable manual Queue brief as the fallback when no client connects.
- Generate `/ai.txt` tool/scope guidance from the executable contract so names
  cannot drift.
- Add exact client setup steps only after that client passes the complete
  lifecycle; otherwise state the capability requirement only.

## Next safe action

After the grant and result contracts stabilize, design the smallest connection
center around the real grant fields and failure states. Do not create a mock
control whose effect is not enforced by the MCP edge.

## Definition of done

- [x] A person can understand one connection and its authority without reading
      OAuth or MCP documentation. A check script fails the build if that
      vocabulary reaches the page.
- [x] Scope reduction and revoke change real server behavior, not only UI state.
- [x] Activity is useful and private-content-safe.
- [x] Manual Queue handoff remains usable before and after connection failure.
- [x] `/ai`, `/ai.txt`, tool catalog, scope catalog, and verified-client truth
      are generated or contract-checked for parity.
- [ ] Desktop/phone, keyboard, focus, reduced motion, overflow, empty, error,
      expired, revoked, and stale-tool states pass browser review. Built to the
      responsive and 44px-target contract and rendered in a check script, but
      **not yet opened in a browser** — see residual risk.

## Evidence

- `app/ai/page.tsx` and `app/ai.txt/route.ts` are the current public setup
  surfaces.
- `components/api/ApiKeyManager.tsx` is evidence of a separate API-key lifecycle,
  not an OAuth connection center.
- `docs/planning/family-history-bring-your-ai-alignment.md` defines the grant,
  connection, and client-truth contract.

## History

- 2026-08-16 · Codex — created from the adopted Bring Your AI audit without
  changing public copy or implying a connection control already exists.

## Current truth — 2026-08-16 (Claude, execution)

`/app/settings/ai` exists. A person can see each connected assistant — the name
it chose for itself (said plainly to be a label, not proof), how it identified
itself, the record boundary, the exact permissions in the catalog's own words,
expiry, last use, and use count — approve a pending request through a real
consent screen, narrow an active grant, revoke it with a reason, read recent safe
activity, follow stale-tool recovery, and copy a manual brief for an assistant
that cannot connect at all.

The consent screen is rendered from `lib/mcp/catalog.ts`, the module the MCP edge
enforces with, so a permission cannot appear here that the server does not honour
and an enforced permission cannot go unexplained.

`/ai` and `/ai.txt` now both generate their tool list, permission list, and both
never-lists from that same catalog. No client is named.

## Evidence — 2026-08-16

- `app/app/settings/ai/page.tsx`, `components/ai/AiConnectionCenter.tsx`,
  `app/api/ai-connections/route.ts`, `lib/mcp/connectionApi.ts`.
- `scripts/check-connection-center.ts` — renders the real component and asserts
  every catalog scope, both never-lists, every boundary choice, the
  immediate-revocation promise, that nothing is pre-ticked, and that no OAuth
  vocabulary reaches a person. In `pnpm verify`.
- `scripts/check-public-ai-truth.ts` — `/ai.txt` parity with the catalog and the
  no-named-client rule across 18 public files. In `pnpm verify`.
- Navigation entry added to both halves of the two-file contract.

## Residual risk — 2026-08-16

The page has been rendered to static markup in a check script and built, but not
opened at 1280px or 390px in a real browser. Keyboard, focus, and reduced-motion
review are therefore unverified.

## Next safe action — 2026-08-16

Open `/app/settings/ai` at both widths with a pending and an active connection
present and complete the browser review.

## History

- 2026-08-16 · Claude — built the connection centre, generated the public
  surfaces from the enforced catalog, and added the two check scripts that keep
  them from drifting.

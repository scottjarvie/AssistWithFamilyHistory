---
id: AWF-0034
title: Make chosen-AI access revocable immediately
status: needs-you
type: product-change
area: Privacy, Trust & Quality
priority: P1
horizon: Now
gap-kind: OAuth access-token revocation
verdict: production OAuth works; issued Clerk JWT access tokens remain valid until expiry
contract: "Project Philosophy · revocable chosen-AI authority and private family tenancy"
created: 2026-08-12
updated: 2026-08-12
updated-by: Codex
---

## Why this exists

A person must be able to disconnect a chosen AI without waiting for an already
issued bearer token to expire. Production acceptance proved client deletion and
session cleanup, but Clerk's revocation endpoint explicitly rejects JWT access
tokens. The tested token remained valid for about twenty-four hours.

## Current truth

The disposable PKCE client, refresh path, product session, local token copy, and
all synthetic Family History records were removed. The deleted client cannot
mint new tokens. The already issued access JWT nevertheless continued to pass
offline issuer/signature/expiry checks after client deletion because the MCP
edge has no active-client or token-revocation lookup.

No other person or family was involved, and the only retained identity has an
empty private vault. This is a capability gap discovered through safe live
acceptance, not evidence of unauthorized access.

## Next safe action

Choose and approve one immediate-revocation design before broad chosen-AI
availability. Keep the existing issuer, tenant checks, and narrow default
identity scopes unchanged until that decision is implemented and tested.

## Why Scott is needed

The smallest provider option changes the production Clerk OAuth token posture
from signed JWT access tokens to opaque tokens. That is a material security and
compatibility decision across every future OAuth client, not a routine local
implementation detail.

## Smallest decision or action

Approve a focused design spike comparing Clerk opaque tokens with an
application-owned active-client/revocation check, then implement only the
recommended path through a protected Work Order.

## Recommendation

Prefer Clerk's standard opaque access-token mode if it preserves stateless MCP
client compatibility and gives the resource server an authoritative active
token check. It matches the product promise most directly. If provider limits
or request latency make that infeasible, add a narrow server-side revocation
registry keyed by token/client identifiers with bounded caching.

## Alternatives and trade-offs

- **Opaque provider tokens:** strongest standard revocation semantics and least
  custom security code, but changes token validation and may add provider calls.
- **Application revocation registry:** retains local JWT verification and can
  revoke exact clients/tokens, but creates security-critical lifecycle state and
  cache-consistency work.
- **Accept expiry-only cleanup:** no implementation cost, but contradicts the
  durable chosen-AI revocation promise for up to the configured token lifetime.

## What each choice changes

- Opaque tokens change the production provider and resource-server validation
  path for OAuth clients.
- A registry adds product-owned revocation state, checks, retention, and tests.
- Expiry-only leaves deletion of a client or connection non-immediate.

## Safe default

Do not enable broad OAuth client availability. Continue isolated acceptance
only with labeled identities, disposable clients, empty workspaces, and secure
destruction of every token copy.

## Consequence of waiting

The first MCP foundation remains useful for narrow soft-launch testing, but it
must continue to state revocation as Partial and should not invite arbitrary
clients or sensitive family-data use.

## Constraints

- Do not weaken exact issuer, access-token type, expiry, subject, client-ID, or
  server-derived owner checks.
- Do not broaden scopes, expose secrets, inspect real family data, or use Scott's
  account for testing.
- Treat token-format/provider changes as normal protected software plus an
  explicit production security decision.

## Evidence

- `docs/operations/family-history-mcp-production-acceptance-2026-08-12.md`.
- Clerk revocation response: HTTP 400 `unsupported_token_type`, stating that JWT
  access tokens cannot be revoked.
- After disposable client deletion, the already issued token still received a
  successful authenticated MCP tool-list response.

## Definition of done

- [ ] Scott approves the production revocation posture.
- [ ] Disconnecting an OAuth client or exact token makes the bearer unusable
  within a documented short bound.
- [ ] Refresh, reconnect, client deletion, token revocation, and post-revocation
  MCP denial pass with an isolated synthetic identity.
- [ ] Tenant isolation and ordinary session-JWT rejection remain proven.
- [ ] Human and agent docs state the resulting Current / Partial boundary.
- [ ] Protected PR, CI, production deployment, cleanup, and independent audit
  evidence remain separate.

## History

- 2026-08-12 · Codex production acceptance — created from the live Clerk
  `unsupported_token_type` result after a successful PKCE/MCP/UI lifecycle and
  deletion of the disposable client, session, local token copy, and fixtures.

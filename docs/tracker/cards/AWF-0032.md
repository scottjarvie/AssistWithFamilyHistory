---
id: AWF-0032
title: Make Queue tenancy and expiry fail closed
status: done
type: product-change
area: Privacy, Trust & Quality
priority: P0
horizon: Now
gap-kind: security and authority repair
verdict: implemented
contract: "Core §§4–7 · Queue tenancy, authority expiry, and recovery"
created: 2026-08-12
work-orders: AWF-WO-004
updated: 2026-08-12
updated-by: Codex
---

## Why this exists

A private family-history Queue must never rely on a deployment mode to decide
whether an anonymous or cross-owner caller may continue. An expired AI handoff
must also stop authority predictably and remain recoverable without leaving a
stale deadline that immediately blocks the newly chosen AI.

## Current truth

The 2026-08-12 independent audit reproduced that public Queue functions used
the shared tenant guard whose legacy `shadow` mode logs a denial but returns a
decision. It also found that the expiry mutation had no scheduler/read wiring,
AI continuation checked only lease expiry after claim, and assign/resume/reopen
could retain an expired handoff deadline. The focused repair merged through
protected PR #32 as main commit
`1a82a03ed207c8cd3c9e99f0dd4b6d4720c282cd`. Exact-main Actions and Vercel
production deployment passed. Provider-specific Convex build-plan evidence,
authenticated production behavior, and the separate final audit remain
unverified and stay isolated in AWF-0030 and AWF-WO-004 rather than being
inferred from deployment success.

## Scope

- Add a Queue-local fail-closed guard for every public Queue read and mutation
  without changing the broader trust-boundary provider policy.
- Schedule idempotent lifecycle expiry for new handoff/lease deadlines and
  reconcile missed or pre-existing deadlines during bounded reads.
- Prevent chosen-AI leases and continuation commands from outliving handoff
  authority.
- Make assign, resume, and reopen clear or explicitly renew stale expiry while
  preserving honest disconnected behavior and exactly four product states.
- Add adversarial Convex fixtures for anonymous/cross-owner shadow-mode calls,
  scheduler/read reconciliation, bounded authority, and recovery.

## Next safe action

Run the independent Work Order audit against merged main. Continue the separate
AWF-0030 provider and authenticated synthetic proof only with isolated test
access; do not inspect or mutate real genealogy data.

## Constraints

- No final Queue UI, navigation, MCP endpoint, provider/auth policy change,
  production data, credential, secret, billing, DNS, or migration.
- Expiry scheduling is lifecycle housekeeping only; it must never run family
  research or imply a product-owned autonomous worker.
- Preserve private Queue directives/activity and all unrelated dirty work.

## Evidence

- Source: `convex/queue.ts`, `lib/queue/contract.ts`.
- Adversarial runtime fixtures: `convex/queue.test.ts`.
- Pure lifecycle fixtures: `scripts/test-queue-contract.ts`.
- Initial local proof: 16/16 Queue Convex tests, 100/100 repository unit tests,
  TypeScript, and diff checks pass on 2026-08-12.
- Full local `pnpm verify`: 47/47 checks passed on 2026-08-12.
- Protected PR #32 merged normally as
  `1a82a03ed207c8cd3c9e99f0dd4b6d4720c282cd`; exact-main Actions run
  `31615964828` passed `build-and-smoke`.
- Vercel production deployment `HDzMFmbrsany1z95y6y63r3vSHgb` reported Ready,
  Current, and sourced from that exact main commit. A signed-out public homepage
  probe returned HTTP 200.
- The provider build-plan line, authenticated multi-client Queue journey,
  revocation/cleanup, and independent audit remain unverified and must not be
  inferred from the successful Next.js deployment.

## Definition of done

- [x] Anonymous and cross-owner public Queue calls fail in both shadow and enforce modes.
- [x] Queue denial cannot read or mutate the targeted item.
- [x] Handoff/lease expiry schedules and reconciles one attributable transition into Needs You.
- [x] AI leases cannot outlive the handoff and late continuation commands fail.
- [x] Reassignment/resume/reopen cannot preserve a stale authority deadline.
- [x] Adversarial Convex and pure contract fixtures pass locally.
- [x] Protected PR and required CI pass without weakening safeguards.
- [x] Deployment/provider/authenticated/revocation/cleanup proof remains separately truthful.
- [ ] A separate AI records the final audit result; Work Order audit is explicitly not-audited.

## History

- 2026-08-12 · Codex Queue audit — created from selectively verified P0 tenant
  and expiry/recovery findings against current `main` and PR #31 evidence.
- 2026-08-12 · Scott — approved the smallest protected backend tranche with no
  UI, external MCP, provider, production-data, migration, or tracker fast lane.
- 2026-08-12 · Codex — implemented the scoped source and adversarial local tests;
  protected PR/CI and all external proof layers remain in progress.
- 2026-08-12 · Codex — merged protected PR #32, observed exact-main Actions run
  `31615964828` pass, and verified Vercel production deployment
  `HDzMFmbrsany1z95y6y63r3vSHgb` Ready on the matching main commit. Kept the
  provider/authenticated proof and independent audit explicitly separate.

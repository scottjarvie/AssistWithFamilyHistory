---
id: AWF-0046
title: Give family evidence a private, exact-version B2 foundation
status: doing
type: product-change
area: Chosen AI & Durable Work
priority: P1
horizon: Current
gap-kind: adopted media storage contract not yet applied
verdict: reviewed evidence bytes work from private Convex Storage, but exact B2 manifests, safe renditions, and provider proof do not exist
contract: "family evidence uses B2 bytes behind Convex authority, with source-exact restricted originals where required, clean normal renditions, exact versions/hashes, replay-safe sessions, and exact cleanup"
dependencies: AWF-0045
created: 2026-08-30
updated: 2026-09-03
updated-by: Codex
work-orders: AWF-WO-012
---

## Why this exists

AWF-0045 closed the urgent gap: a reviewed scan can reach a chosen AI as real
bytes. The family standard adds the durable storage and lifecycle guarantees
that Convex `storageId` alone does not express. Family-history evidence also
needs a deliberate distinction between a source-exact archival artifact and
the cleaned rendition safe for normal viewing and AI use.

## Current truth

- Person uploads land in private Convex Storage and new/replaced files become
  Private, unreviewed, and unavailable to AI.
- `family_history_get_evidence` applies owner, boundary, living/review,
  `aiUseAllowed`, rights, and byte-budget gates.
- The baseline row had no B2 version/hash/derivative manifest or durable upload
  session. The active branch replaces the unused public-URL helper with a
  private, exact-version evidence path; deployed rows remain unchanged.
- Existing files and imported references are real product state. This Proposed
  Card authorizes no provider action or migration.
- The active branch now has the additive exact-manifest/session control plane,
  fail-closed private/public bucket bindings, source-exact private image
  originals, three metadata-clean renditions, exact-version/hash/type/length/
  decode checks, partial-write cleanup, and structured camera/GPS proposals.
- The production code and Convex schema/actions are deployed after the native
  `sharp` package hotfix.
- Backblaze now has exactly two project buckets: a private evidence bucket and
  a public derivative bucket. Both keep all versions and use default server-
  side encryption; object lock remains off. Separate read/write application
  keys are restricted to their corresponding bucket.
- The production Convex deployment now holds the two bucket endpoints,
  regions, names, and credentials plus the production storage-mode and public-
  origin bindings. Secrets were not copied into Vercel or repository files.
- A production-only internal action wrote and reread one clearly marked
  synthetic object in each bucket, verified exact byte length and SHA-256,
  removed each exact version, and then listed zero retained versions or delete
  markers under both run prefixes. Each credential was also denied when used
  to list the other bucket.

## Next safe action

Extend the validated rendition policy to the remaining media classes, adopt
the exact-version path for person uploads, and prepare a migration dry-run that
compares counts and hashes without changing any current byte.

## Constraints

- Private only. Public story derivatives remain a separate human publication
  program.
- Storage keys contain no person, family, place, title, living status, or
  relationship text.
- Ordinary UI/MCP reads never receive the restricted archival original.
- Upload never sets review, AI-use permission, rights approval, fact truth,
  conflict settlement, or publication.
- No silent fallback to Convex Storage or the public-URL B2 helper.

## Completion evidence

- Tests cover exact manifests, checksum/type/full-decode mismatch,
  metadata/provenance extraction, clean renditions, replay/conflict, expiry,
  concurrent finish, superseded versions, delete, and no-fallback.
- Owner/people/work boundary and living-person refusals stay uniform.
- Provider inventory, exact byte/hash verification, cross-bucket credential
  denial, and zero marked residue are recorded without exposing secrets.
- A migration dry-run compares exact counts/hashes and changes no current byte.

On 2026-09-02, local tests passed for exact manifests, byte/type/hash/decode
validation, clean renditions, bounded partial-write cleanup, session replay,
single-finalizer concurrency, and revoke-before-finish. Typecheck, lint,
production build, tracker parity, and all media/trust/API boundary contracts
also passed. Provider, migration, deployed-byte, and operational cleanup proof
remain deliberately unclaimed.

On 2026-09-03, Backblaze bucket creation, encryption/versioning posture,
separate bucket-scoped keys, production Convex bindings, and deployment of the
internal provider check were visually verified. Full local verification passed
58 commands, pull request #72 passed GitHub and Vercel checks, and post-merge
main CI passed.

The final production run after pull request #73 returned `ok: true` for a
144-byte object in each bucket with matching SHA-256
`106f90223a462f3d84fe89f1069a976d6423eb79a1f7eca0ebab136622812b6d`.
Both exact versions reported cleanup, both post-cleanup version counts were
zero, and each bucket-scoped credential was denied when pointed at the other
bucket. No real family byte or other project's resource was involved.

## History

- 2026-08-30 · Codex — proposed as the standards-based successor to AWF-0045,
  preserving its deployed evidence-read value and live-proof residue.
- 2026-09-02 · Codex — started the approved foundation and verified its pure
  and Convex control-plane tests locally. The remaining outcome is provider
  inventory/configuration, non-image rendition policy, person-upload adoption,
  migration dry-run, deploy/live proof, and exact operational cleanup proof.
- 2026-09-02 · Codex — deployed the foundation through pull requests #70 and
  #71 after repairing Convex native `sharp` packaging. Code/schema deployment
  is proven; bucket/key configuration and provider-backed byte proof remain.
- 2026-09-03 · Codex — created the private/public Family History buckets with
  encryption and version retention, created separate bucket-scoped keys, bound
  them only to production Convex, and merged/deployed pull request #72 with a
  guarded synthetic provider check.
- 2026-09-03 · Codex — strengthened the checker in pull request #73, passed all
  local/PR/main checks, and completed the live exact-byte, cross-credential-
  denial, exact-version cleanup, and zero-residue provider proof.
